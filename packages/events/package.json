{
  "name": "@ai-platform/events",
  "version": "1.0.0",
  "description": "Shared Redis and BullMQ event backbone for AI platform services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "bullmq": "^5.34.2",
    "ioredis": "^5.8.2",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
